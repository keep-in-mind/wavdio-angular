export class Image {

    filename: string
    alternativeText: string

    constructor(filename: string, alternativeText: string) {
        this.filename = filename
        this.alternativeText = alternativeText
    }
}
